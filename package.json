{
  "name": "maison-han",
  "version": "0.1.0",
  "private": true,
  "description": "MAISON HAN — premium spirits storefront. Static HTML + one Stripe Checkout function.",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  },
  "scripts": {
    "pages:dev": "wrangler pages dev .",
    "pages:deploy": "wrangler pages deploy .",
    "cf-pages-install": "npm install --omit=dev"
  }
}
